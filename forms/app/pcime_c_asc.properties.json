{
    "context": {
        "expression": "user.role === 'chw' && user._id != contact._id && summary.alive && summary.isFamilyPerson && !summary.muted && (contact.type === 'person' && (!contact.date_of_birth || (ageInDays(contact) >= 60 && ageInDays(contact) <= 1825)))",
        "person": true,
        "place": false
    },
    "icon": "family",
    "title": "PCIME-C"
}