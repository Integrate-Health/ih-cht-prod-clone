{
    "context": {
        "expression": "user.role === 'chw' && user._id !== contact._id && summary.alive && summary.is_family_person && !summary.muted && summary.show_pregnancy_form && contact.type === 'person' && ageInYears(contact) >= 12 && ageInYears(contact) < 60 && (contact.sex === 'female' || contact.sex === 'femme' || contact.sex === 'Femme')",
        "person": true,
        "place": false
    },
    "icon": "family",
    "title": [
      {
        "locale": "en",
        "content": "Pregnancy and Family Planning"
      },
      {
        "locale": "fr",
        "content": "Grossesse et PF"
      }
    ]
}