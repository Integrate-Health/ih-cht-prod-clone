{
    "context": {
        "expression": "user.role === 'chw' && user._id !== contact._id && summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInYears(contact) >= 12 && ageInYears(contact) < 60 && (contact.sex === 'female' || contact.sex === 'femme' || contact.sex === 'Femme')",
        "person": false,
        "place": false
    },
    "icon": "family",
    "title": [
      {
        "locale": "en",
        "content": "FP Followup (renewal)"
      },
      {
        "locale": "fr",
        "content": "Renouvellement de méthode PF"
      }
    ]
}