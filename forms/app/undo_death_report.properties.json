{
    "context": {
      "expression": "user.role === 'chw' && user._id !== contact._id && !summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInYears(contact) < 100",
        "person": true,
        "place": false
    },
    "icon": "icon-person",
    "title": [
        {
          "locale": "en",
          "content": "Undo death report"
        },
        {
          "locale": "fr",
          "content": "Annuler le rapport de décès"
        }
      ]
}
