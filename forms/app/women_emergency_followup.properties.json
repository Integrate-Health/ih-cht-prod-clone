{
    "context": {
        "expression": "user.role === 'chw' && user._id != contact._id && summary.alive && summary.isFamilyPerson && summary.show_women_emergency_form && !summary.muted && (contact.type === 'person' && (!contact.date_of_birth || ((ageInYears(contact) >= 12 && ageInYears(contact) < 60) && (!contact.sex || contact.sex === 'female' || contact.sex === 'femme' || contact.sex === 'Femme'))))",
        "person": true,
        "place": false
    },
    "icon": "treatment",
    "title": [
      {
        "locale": "en",
        "content": "Women Emergency Follow Up"
      },
      {
        "locale": "fr",
        "content": "Suivi d'urgence des femmes"
      }
    ]
}