{
  "expression": "user.role === 'reco' && user._id !== contact._id && summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInYears(contact) < 150",
  "person": true,
  "place": false
}
