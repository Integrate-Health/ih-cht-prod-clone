{
  "expression": "user.role === 'chw' && user._id !== contact._id && summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInDays(contact) <= 1825",
  "person": true,
  "place": false
}
